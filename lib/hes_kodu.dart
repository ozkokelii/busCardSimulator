import 'package:flutter/material.dart';

class Hes extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Hes Kodu Tanımlama"),
      ),
      body: Center(
        child: Text("HES KODU TANIMLANACAK"),
      ),
    );
  }
}
