import 'package:flutter/material.dart';

class Konumum extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Konum"),
      ),
      body: Center(
        child: Text("KULLANICI KONUMU GÖSTERİLECEK"),
      ),
    );
  }
}
