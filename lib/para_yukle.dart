import 'package:flutter/material.dart';

class ParaYukle extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Para Yükle"),
      ),
      body: Container(
          child: Center(
        child: Text("PARA YÜKLEME YERİ"),
      )),
    );
  }
}
