import 'package:flutter/material.dart';

class Sikayet extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Sikayet ve Dilekler"),
      ),
      body: Center(
        child: Text("ŞİKAYET VE DİLEK SAYFASI"),
      ),
    );
  }
}
