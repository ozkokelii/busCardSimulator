import 'package:flutter/material.dart';

class Seferler extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Sefer Sorgulama"),
      ),
      body: Center(
        child: Text("OTOBÜS SEFER SORGULAMA SAYFASI"),
      ),
    );
  }
}
